<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Password Update</title>
    <!-- Add SweetAlert library -->
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
</head>
<body style="font-family: Arial, sans-serif; background-color: #f0f0f0; margin: 0; padding: 0;">
    <div style="background-color: #ffffff; border-radius: 5px; box-shadow: 0 0 10px rgba(0, 0, 0, 0.2); max-width: 500px; margin: 50px auto; padding: 20px; text-align: center;">
        <h1 style="color: #333;">Password Update</h1>
        <form action="/auth/update-password" method="POST" onsubmit="return handleFormSubmit()">
            <input type="hidden" name="token" value="<%= token %>">
            <div style="margin: 20px 0;">
                <label for="newPassword" style="display: block; font-weight: bold; margin-bottom: 5px;">New Password</label>
                <input id="newPassword" type="password" name="newPassword" required style="width: 80%; padding: 10px; border: 1px solid #ccc; border-radius: 3px; font-size: 16px;">
            </div>
            <div style="text-align: center;">
                <button class="btn blue" type="submit" style="background-color: #007bff; color: #fff; border: none; border-radius: 3px; padding: 10px 20px; cursor: pointer; font-size: 16px;">Update Password</button>
            </div>
        </form>
    </div>

    <!-- JavaScript code to trigger SweetAlert -->
    <script>
        function handleFormSubmit() {
            // Replace this with your actual form submission logic
            // Here, we simulate a successful password update
            // You can replace this with your server-side validation and logic
            // If the update is successful, show a SweetAlert success message
            Swal.fire({
                icon: 'success',
                title: 'Password Updated',
                text: 'Your password has been successfully updated.',
            }).then(function() {
                // Redirect to the home page or any other desired page
                window.location.href = "/";
            });

            // Prevent the form from submitting
            return false;
        }
    </script>
</body>
</html>
